# Dependencies for the interactive research dashboard (dashboard/app.py).
# Pinned to the versions the dashboard was built and tested against.
#
# Streamlit Community Cloud reads this file from the repository root.
#
# Local install:   pip install -r requirements.txt
# Local run:       streamlit run dashboard/app.py

streamlit==1.63.0
pandas==3.0.5
plotly==7.0.0
